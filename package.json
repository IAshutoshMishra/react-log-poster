{
  "name": "react-log-poster",
  "version": "1.0.0",
  "description": "A lightweight React package to POST logs to a fixed API with environment config and in-memory retry.",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist --extensions \".js,.jsx\"",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["react", "logging", "post", "retry", "axios"],
  "author": "Ashutosh",
  "license": "MIT",
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "dependencies": {
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@babel/cli": "^7.26.0",
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "@babel/preset-react": "^7.26.3"
  }
}
